import { CharacterPagePipe } from './character-page.pipe';

describe('CharacterPagePipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterPagePipe();
    expect(pipe).toBeTruthy();
  });
});
